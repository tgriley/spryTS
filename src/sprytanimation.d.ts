export declare class SprytAnimation {
    Name: string;
    Row: number;
    FirstFrameIndex: number;
    LastFrameIndex: number;
    constructor(name: string, row: number, firstFrameIndex: number, lastFrameIndex: number);
}
